description = 'COMPROT Search Engine'

allprojects {
    group = 'de.comprot'
    version = '1.0.0-SNAPSHOT'
}

subprojects {
    apply plugin: 'groovy'

    repositories {
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}
