<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:p="http://www.springframework.org/schema/p"
        xmlns:task="http://www.springframework.org/schema/task"
        xsi:schemaLocation="
            http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.0.xsd
            http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">

    <task:scheduler id="scheduler" pool-size="10" />
    <task:executor id="executor" pool-size="10" />

    <bean id="adminUserCheckJobDetail" class="org.springframework.scheduling.quartz.JobDetailFactoryBean"
        p:jobClass="de.comprot.task.AdminUserCheckJob" p:durability="true" />

    <bean id="adminUserCheckJobTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerFactoryBean"
        p:jobDetail-ref="adminUserCheckJobDetail" p:startDelay="0" p:repeatCount="0" />

    <bean id="proteinIndexingJobDetail" class="org.springframework.scheduling.quartz.JobDetailFactoryBean"
          p:jobClass="de.comprot.task.ProteinIndexingJob" p:durability="true" />

    <bean id="proteinIndexingJobTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerFactoryBean"
          p:jobDetail-ref="proteinIndexingJobDetail" p:startDelay="0" p:repeatCount="0" />

    <!--
        Most of the following is taken from:
        http://www.objectpartners.com/2013/07/09/configuring-quartz-2-with-spring-in-clustered-mode/
    -->

    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="jobFactory">
            <bean class="de.comprot.task.AutowiringJobFactory"/>
        </property>
        <property name="triggers">
            <list>
                <ref bean="adminUserCheckJobTrigger" />
                <ref bean="proteinIndexingJobTrigger" />
            </list>
        </property>
    </bean>

</beans>
